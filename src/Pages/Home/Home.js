import React from 'react';
import HeroArea from '../HeroArea/HeroArea';
import Navbar from '../Navbar/Navbar';

const Home = () => {
    return (
        <div>
            <Navbar></Navbar>
            <HeroArea></HeroArea>
        </div>
    );
};

export default Home;